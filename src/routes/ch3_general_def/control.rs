pub fn control() {
    let a = [1, 2, 3, 4, 5];
    for element in a {
        println!("for element in a : {element}" );
    }

    for number in 1..5 {
        println!("for number in (1..5) : {number}");
    }
}